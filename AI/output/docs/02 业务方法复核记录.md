# 业务方法复核记录（SRC-023/024）

日期：2026-09-18；范围：一期编码质量整改，非独立CR。全量JDK语法树清单为[quality-method-inventory.csv](verification/quality-method-inventory.csv)，含248业务源文件、652方法/构造器、345多语句流程块；JavaParser基线及当前清单独立保留，统计口径注明。复杂/纯装配构造器、简单getter、单语句业务分派、catch/finally不能用流程块数字替代复核。

## 1. 全部源码的职责分类复核

| 分类 | 核验内容 | 处理/证据 |
|---|---|---|
| HTTP输入与身份/流式输出 | 命令/响应转换、Cookie/SID/CSRF、失败状态、连接取消、回调编号与生命周期 | 主入口50项AST审计0失败；身份隔离与异常回滚测试，真实多设备验收待06 |
| 应用服务与Flow/Graph/后台任务 | 权限与归属、输入窗口、事务结果、幂等、租约/代次、模型调用证据、对象状态方法、阶段分离 | 无直接Entity字段构造；提取/索引/文件长方法分解；重点链路见下表 |
| 17类Entity与Aggregate | 初始化、不可迁移归属、版本、删除状态、唯一来源、语义转换、无重复聚合标量与仓储依赖 | 规则回收到实体；聚合语义调用；DomainModelQualityTest覆盖版本/归属等反例 |
| DomainService/仓储 | 通过聚合验证更新；PO转换与保存分开；查询列白名单、有界窗口、CAS；内部失败不伪报成功 | domain/service展开entity=0；17个仓储固定映射；没有增加泛用映射/继承框架 |
| Value/构造器 | 预算边界、查询数量、条件复制、事实原文来源、会话凭据防御复制；注入构造器仅装配 | 业务构造器补充规则步骤，纯依赖装配不凑编号；防御复制测试 |
| 外部适配/安全/文件 | 配置缺失、超时/响应、并发/字节/页数/文本容量、向量结构/归属过滤、静态状态与资源释放 | 内部解析/嵌入/向量批次按职责拆分；全入口异常审计，真实供应商联调未执行 |
| DTO/Param/DO/PO/Mapper/错误类型/配置 | 属性说明与契约、只读映射/简单getter、参数名、框架隔离、依赖字段例外 | 默认Checkstyle覆盖规定语法；简单访问不制造多步假流程；POM/SQL/职责不由其自动证明 |
| React界面与消息历史 | 生命周期/取消/游标、全量历史替换、版本与排序、共享状态边界、异常反馈 | MessageHistory负责合并决策；2项行为测试，类型/生产/格式检查 |

## 2. 重点链路的职责、正常与反例

| 业务方法/对象 | 原来→现在 | 不变量与反例 |
|---|---|---|
| ChatFlow.submit/MessageAggregate/ChatRunAggregate | App全字段构造→用户输入/助手占位/接受任务领域工厂 | 同一用户、对话、角色、稳定序号与已保存消息；跨账号/未保存消息拒绝 |
| AuthFlow.login/refresh/LoginSessionAggregate | App凭据和状态拼装→设备与会话语义签发/轮换 | 同设备撤销旧会话、其他设备有效；SID/认证代次/CSRF/刷新重放不能跳过，凭据防御复制 |
| ConversationEntity.started/rename | 创建后重复rename→创建与重命名复用normalizedTitle | 空归属、空白/超长标题拒绝；首尾空白规范化，初始化序号/代次固定 |
| TravelWriteDomainService.save*/Entity.assertUpdateAgainst | DomainService展开实体条件→聚合委托本实体核验 | 版本匹配、主身份/归属不变、适用实体删除不可恢复；CAS失败终止事务 |
| MemoryFlow.extract/reserveExtraction/recordExtraction/persistFacts | 单长流程→登记、调用、记账、事实保存四个责任阶段 | 先登记再外部调用，重放不自动再计费；来源/版本/代次有效才提交事实 |
| MemoryFactAggregate/FactProposalValue | 应用散落类别/原文条件→领域已核验提案 | 固定类别/键/长度，只允许连续原文摘录；非法类别和幻觉提案不采纳 |
| KnowledgeIndexer/IndexJobAggregate/KnowledgeChunkAggregate | 单长索引→领取、批次、删除補偿、终态；分块一次生成公开ID | 代次/租约/栅栏检查保留；向量Key与公开ID同源；重复领取拒绝 |
| TravelGraph/RunCoordinator/ContextBudgetValue | 显式节点协调、持久检查点、流式节流与预算模型各有职责 | 旧图版本/任务代次/栅栏拒绝提交；压缩后仍超预算则拒绝模型输入；usage与估算区分 |
| FileOutAdaptorImpl/BoundedTextWriter | 落盘混合解析→有界扫描/解析/原子保存 | 解析槽、10MB、50页、正文容量与私有路径，资源释放由对应finally承接 |
| 17个RepositoryImpl | 映射与保存同方法→固定toPersistence再新增/CAS | 全字段显式映射，保存条件与原版本一致；不对已有业务模型套无用框架 |
| MessageHistory.merge/snapshot | 页面Map+排序→历史对象控制版本与序号 | 迟到版本不覆盖新内容、全量重载清除旧记录、大整数排序和外部修改隔离 |

## 3. 当前验证及剩余边界

后端16项、前端2项测试与构建通过。新业务门禁反向样例测试编号缺失/跳号/非中文、私有长方法、应用new Entity和DomainService展开entity；安装冲突验证原配置不被覆盖。既有四层异常门禁和提交失败/回滚回归保留。自动编号/长度/有限结构没有类型与完整业务语义证明能力，重点链路反例也不构成全部企业生产场景的证明。

本轮没有执行真实模型/嵌入/高德/邮件、多设备与用户全流程验收、负载/长期运行、真实故障恢复或独立CR。这些保持后续既定阶段状态，不用“已全面扫描”隐去尚未执行的验收。已执行SQL迁移SHA保持不变。
