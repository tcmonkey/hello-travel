# 全项目业务职责复核（SRC-030/031当前）

日期2026-09-18；05编码整改及针对性回归，非独立CR。此前转换/异常复核[保留归档](<history/02 业务方法复核-SRC025029.md>)；其“全部职责复核”未识别业务与模式维度错误，本轮明确纠正。

## 1. 模式与业务的两个维度

先识别业务边界，再在每个业务内按用例需要选写入、域内读、外部读、规则计算、纯计算。DomainService归业务，本域可有多个服务，不强制每模式一个服务。当前业务有五个子域，数量碰巧与模式数量相同，不能据此建立对应关系。

| 业务 | 实际领域服务 | 保存/清理 | 应用职责 |
|---|---|---|---|
| auth | AuthDomainService | 5保存+RefreshReceipt清理 | 注册、登录、刷新、退出、重置及保留任务直接调用；AuthDomainParamAssembler只转换对应聚合参数 |
| chat | ChatDomainService | 4保存 | 对话动作、运行协调和记忆流程直接调用；ChatDomainParamAssembler按Conversation、Message、Run、Invocation明确转换 |
| memory | MemoryDomainService | 3保存+MemoryFactSource清理 | 记忆提取、压缩和隐私清理直接调用；MemoryDomainParamAssembler按记忆对象明确转换 |
| knowledge | KnowledgeDomainService | 3保存 | 上传、索引、重试和删除流程直接调用；KnowledgeDomainParamAssembler按文档、分块、任务明确转换 |
| sync | SyncDomainService | 2保存+SyncEvent清理 | 事件发布、发件箱派发和保留任务直接调用；SyncDomainParamAssembler按同步事件与发件箱明确转换 |

所有Param归domain/<业务>/model/param，领域服务只依赖同业务仓储。原全局TravelWriteDomainService、DomainWrites、TravelWriteApplicationAssembler、TravelRepositories和旧persistence包已删除，没有兼容路由。清理14个无调用物理删除入口及Param，保留20实际操作；WriteDO仅是共享结果标记，ApplicationFailures.required仅验证技术失败契约。

## 2. 全项目范围与完整链路

当前源码由项目门禁扫描369类、924方法/构造器和398个多语句流程块，违规0；[当前方法清单](verification/current-method-inventory.csv)随SRC-063重新生成。SRC-031的vertical清单作为当轮历史证据保留，不再代表当前源码。输入/应用/输出/持久化映射边界、层内异常、编号和方法规模继续受检；静态分类不是语义证明。

AuthFlow使用auth写/读协作，SyncEvents单独接sync协作；Transactions以auth账号序列协调提交。ChatFlow域内普通读直接chat仓储、写入经chat领域；MemoryFlow/PrivacyCleanup在应用层显式协同memory和chat；后台保留流程显式协同auth和sync；知识检索/索引在应用层使用knowledge与auth读取及外部端口。各域服务无其他域仓储，无中央路由；Spring离线装配验证没有循环依赖。

20个保留操作的规则方法体AST与拆分前相同，既有归属、版本、删除状态、目标缺失与CAS保持。17领域实体、仓储接口和PersistenceConverter未改，正式MySQL V1 SHA不变。当前结构改变所有相关消费者与装配，并删除未使用依赖，避免只重命名而维持隐藏的大总服务。

## 3. 当前验证与限制

39项后端JUnit全部通过（旧35+新4个测试方法）；新测试覆盖5域依赖/Param归属、全部17保存的创建/更新/拒绝/CAS/不可用、3清理结果和失败、应用跨auth与sync的正常commit和失败rollback。跨域回归使用真实TransactionTemplate及本域服务/协作、mock事务管理器，证明回滚调用与后续中止，不证明真实MySQL数据回滚。36主入口AST0违规。DDD根与独立临时快照各7项通过，执行逻辑未变；流程10/安装16、独立消费者及94来源/137核心发行一致性通过。

原始证据见[vertical-summary.json](verification/vertical-summary.json)、vertical-junit、vertical-*.log及CSV；源文件归档记录整改前状态。前端本轮没有修改/重跑，不把旧4项称为本轮执行。真实百炼/嵌入/高德/SMTP、多设备全链路、负载/故障、完整06/独立CR/生产部署未执行，已安装MySQL/Milvus/Redis本轮未操作。

## 4. 为什么此前未发现

我把开发模式误用为业务边界，并在后续复核中只确认有限规则和转换位置，未核对域内所有依赖与跨域组织。这是建模和审查遗漏，不能归因为用户没有描述或以静态门禁通过辩解。通用规范1.14的DDD-009～012以业务归属、对象命名与完整链路复核纠正，当前测试提供本项目反例证据，不规定其他项目必须有相同业务域或服务数量。
