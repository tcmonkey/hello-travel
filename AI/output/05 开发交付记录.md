---
project: hello-travel
artifact: 开发交付记录
status: complete
version: "0.3"
mode: full
updated: "2026-09-18"
sources: [SRC-017, SRC-018, SRC-019, SRC-020, SRC-021, SRC-022, SRC-023, SRC-024]
---

# 一期开发交付记录

## 1. 本次交付与阶段状态

已按用户“执行/继续”完成一期代码生成，终点05。React/Ant Design前端、Java17八模块、真实模型/图编排端口、MySQL迁移、Redis频控和Milvus专用集合适配器均有实质实现。没有订单、支付、真实售后执行接口。

development=complete仅代表此次编码节点完成。06完整开发自测、07 CR、08独立测试、09～14发布与效果阶段仍not_started。下面有限检查是编码阶段证据，不把离线Mock检查、应用启动或页面截图算作完整联调。上游方案保留review原审批事实；SRC-017直接实施授权足以推进编码，不补造历史全文审批。

## 2. 任务—代码—证据追踪

路径均相对后端项目根，前端采用同级../hello-travel-app；应用写入统一经TravelWriteDomainService/完整Aggregate/Repository，基础设施使用MyBatis-Plus标准仓储与结构化查询，不编写原生业务SQL/XML。

| 任务 | 编码状态 | 实质代码位置 | 本次证据/后续验收 |
|---|---|---|---|
| TASK-001 工程/依赖 | complete | pom.xml、八模块POM、checkstyle.xml、start/Application与config | 默认install、revision覆盖、独立消费、Bean装配通过；漏洞/支持周期评审待07 |
| TASK-002 协议/错误 | complete | common/result、exception；client请求/响应；adaptor/http/support | 默认门禁通过；401/503路径通过；完整非法输入与日志审计待06 |
| TASK-003 认证 | complete | application/auth/workflow/AuthFlow、domain/auth、infrastructure/auth、adaptor/security及mail/output | Argon2id/OTP/HMAC/AEAD/刷新账本/页面SID代码完成；旧页隔离离线检查通过；真实邮箱及并发联调待06 |
| TASK-004 对话/任务 | complete | application/chat/workflow/ChatFlow、domain/chat、infrastructure/chat；travel/workflow/RunCoordinator | 幂等、稳定问答位置、取消/重试/删除/租约完成；重试/删除不变量检查通过，持久业务联调待06 |
| TASK-005 同步 | complete | application/sync/service、workflow/SyncEvents、jobs/BackgroundJobs；adaptor/http/stream/SyncStreamController | 持久有序事件、SSE、REST补齐及过期恢复完成；多设备与慢连接待06 |
| TASK-006 记忆/预算 | complete | application/memory/workflow/MemoryFlow、PrivacyCleanup；domain/memory/ContextBudgetValue | 近期6轮、来源摘要、明确长期事实、双epoch与有界压缩完成；UTF8/预算边界检查通过，模型质量待06 |
| TASK-007 私有RAG | complete | application/knowledge/workflow/KnowledgeFlow、KnowledgeIndexer、RagFlow；adaptor/file与knowledge/output | 真TXT/MD/PDF解析、真实嵌入/Milvus SDK、generation/hash/owner复核、异步清理完成；实际Milvus和故障注入待06 |
| TASK-008 旅行图 | complete | application/travel/workflow/TravelGraph；adaptor/model/output与travel/output | 真StateGraph、LangChain4j流式/usage、白名单高德天气/驾车工具；SDK签名编译和装配通过，真实接口待06 |
| TASK-009 对话界面 | complete | ../hello-travel-app/src/TravelApp.tsx、AuthPanel.tsx、api.ts、types.ts | tabs/选删/重命名/全量分页/版本合并/虚拟历史/中文Enter完成；类型构建通过，交互验收待06 |
| TASK-010 知识/上下文 | complete | ../hello-travel-app/src/KnowledgePanel.tsx、TravelApp上下文面板 | 上传/明文预览/全文/索引状态/来源；保守占比与实际usage分列完成；空/错误状态编译通过，真实数据验收待06 |
| TASK-011 有限检查/文档 | complete | start/src/test、scripts、README、AI/output/docs/API、04/05、AI/output/docs/verification | 7条离线检查和构建证据已记录；独立CR/产品验收未执行 |

## 3. 实际变更与主动修订

### 3.1 登录及多设备

密码Argon2id，19MiB/t=2/p=1、随机盐；账号/页面登录事实在MySQL。验证码6位、5分钟、最多5次，REGISTER/LOGIN/RESET_PASSWORD隔离，HMAC校验，待投递码使用独立AES-GCM密钥加密。Redis Lua原子频控，缺SMTP/Redis失败关闭，没有固定验证码后门。

服务端签名随机设备Cookie代表浏览器配置；同一浏览器主动新登录撤销旧SID，其他配置/设备并行。每个页面记住自己的SID，访问令牌仅内存，refresh为HttpOnly Cookie。旧页携带新共享Cookie时先拒绝SESSION_REPLACED，不继承新登录，不撤销新SID。清Cookie或隐私窗口被视为新设备实例；不声称识别物理硬件。同页刷新single-flight，refresh轮换消费账本检测重放；重置密码递增全局auth_epoch立即废止所有旧登录。

### 3.2 对话、恢复与同步

所有已接受输入/回答位置在SQL，草稿有界定期更新原助手记录。一账号最多一个进行中run；同UUID同输入幂等，同UUID不同输入409。失败/取消/中断由用户显式重试，沿用原问答位置，提高attempt/fence；过期RUNNING不会偷偷重发收费请求。

tab清单和每个tab全量历史按固定上界分页直至hasMore=false，浏览器缓存不截成近期窗口。消息草稿version与删除historyEpoch分别处理，恢复期间变化从bootstrap原高水位补齐。每用户sync_seq在业务事务中CAS推进，事件和outbox同事务提交；SSE约1秒推送持久引用，客户端拉取当前实体版本。SSE有界，断线REST补齐，过期重新完整恢复；独立发送池避免直接占住后台调度线程。

### 3.3 多层记忆及上下文

原始全量历史与模型窗口分开。短期近期只取本用户/本对话最近6轮完整问答；更早来源滚动摘要记录实际来源范围与序号；长期事实只采用户明确“请记住”的偏好，摘录须匹配本人未删除USER消息，有90天期限和来源关系。删除推进historyEpoch/memoryEpoch，旧摘要/事实立即不参与模型输入，后台分批清理旧正文。

应用窗口32768、输出4096、安全4096，采用UTF8字节加包装的保守估算，压缩调用每run最多2次，仍超限受控失败。面板显示占比、组成与压缩状态；供应商返回回答usage时额外显示真实输入/输出，不将估算冒充计费量。私密提示只在有界执行内存使用，图checkpoint仅保存runId/revision/node。

### 3.4 RAG、向量与资料

MySQL17张业务表，Milvus一个专用collection `hello_travel_kb_v1_d1024`，Redis频控。ht_knowledge_chunk是MySQL正文/来源/状态清单，Milvus保存1024维派生向量及关联字段；RAG并不强制双存储，这是本项目审计与失效一致性的取舍。

TXT/MD/PDF上限：原文件10MiB、PDF50页、提取UTF8正文2MiB、256块；明确拒绝加密PDF和不支持类型。真实text-embedding-v4批次≤16，索引稳定块键、显式代次、lease/fence和READY发布；召回top12、阈值0.55、最多4来源，逐块SQL复核owner/状态/generation/hash。阈值属于待评估初始值，不是召回质量证明。

向量对账只清比当前快照更老的代次，避免误删未来代次；删除墓碑排除查询并重复清理迟到写。过期索引先恢复再尝试向量清理；受控文件孤儿≥24小时且SQL无引用才删除。真实模型/邮件未知结果不自动重放，只有不计费幂等任务有最多8次退避重试。

Jingdezhen样本见knowledge目录：2024年运营方公开退款个案，明确标注历史日期、渠道与适用边界。不是当前官网通用政策，未自动构造READY文档或向量；完整现行退改条款仍缺项。

## 4. 编码阶段实际检查

构建时本机全局Aliyun镜像请求异常，检查通过临时settings选用Maven Central；没有修改用户全局settings。Java17/Maven3.9.16/Node24.18/npm11.16。下列日志存入AI/output/docs/verification，不依赖临时目录交付。

| 检查 | 实际命令/范围 | 结果与证据 |
|---|---|---|
| 默认构建与安装 | mvn -s /tmp/ht-settings.xml -B -ntp -Dmaven.repo.local=/tmp/ht-independent-repository install | 九项目BUILD SUCCESS；默认Checkstyle零违规，Flatten执行；backend-install.log |
| 离线边界与装配 | 上述install自动执行start模块测试 | 7 tests/0 failures/0 errors/0 skipped：5领域不变量、1旧页共享Cookie隔离、1离线Bean装配；没有调用真实外部服务 |
| revision覆盖 | 同根命令增加-Drevision=0.0.1-verification -DskipTests，package | 真实八模块源码构建成功；测试明确跳过；revision-override.log |
| 独立消费 | 临时无relativePath、独立pom依赖已安装application坐标，clean package | BUILD SUCCESS；项目自身坐标单独安装，第三方缓存复用；independent-consumer.log；未远程发布 |
| 前端 | npm ci；npm run format:check；npm run build | TypeScript/Prettier/Vite生产打包成功；最大JS块约203KiB；frontend-build.log/frontend-format.log |
| Python脚本 | ast.parse两个脚本 | 语法通过，不生成pyc或输出配置值 |
| 最终JAR启动 | java -jar …0.0.1-SNAPSHOT.jar --server.port=18080 --hello-travel.workers.enabled=false | 127.0.0.1临时端口启动，最终Flyway11.20.3校验V1通过；startup-excerpt.log |
| 专用库迁移 | Flyway初始化hello-travel；后续最终JARvalidate | 17业务表＋flyway_schema_history，version=1/success=1，账号=0；mysql-schema.txt；不访问其他业务库 |
| 有限HTTP路径 | GET /actuator/health；缺SMTP POST /auth/challenge；未登录POST /chat/bootstrap | 200 UP / 503 UNAVAILABLE / 401 UNAUTHORIZED；http-error-paths.json；没模拟正常注册登录 |
| 页面预览 | 临时Vite浏览器预览认证界面 | 实际登录/注册/重置界面可展示；没有真实注册或对话生成；临时进程交付前停止 |

完整证据与迁移/lock/JAR SHA-256见[verification目录](docs/verification/README.md)。V1已执行冻结，没有repair、clean、重建用户Milvus集合或Redis清空。密钥读取不进入源码/日志；本地独立密钥存忽略提交的0600 .env.local，无值模板可审阅。

## 5. 已知缺口及后续检查条件

- SMTP尚未配置，注册邮箱验证、验证码登录与密码重置目前不可实际完成；协议与真实投递实现已生成，缺配置明确503。
- 未调用收费模型/嵌入、高德或真实SMTP；百炼窗口/权限/usage、路线时效、Milvus server/schema/搜索、索引质量均未联调。本次未实际创建向量集合。
- 当前完整景区官网退改政策缺项，历史公开个案不能替代。来源正文应由最新正式渠道补充核实。
- 多浏览器/多设备、并发注册/登录/refresh、全量恢复期间删除、慢SSE、提示注入、PDF耗时、数据库回滚、迟到向量写及重启故障组合未执行完整检查。
- 生产TLS、SMTP身份/投递性、最小权限、代理可信IP、参数调优、文件目录容量、消息删除物理回收、终态任务归档和性能/SLA需要06～09明确验证。当前保留登录/任务最小审计记录；不宣称删掉所有受FK关联的审计行。

这些是后续验证或环境资料条件，不将它们报告为通过。无需模拟凭据、验证码、订单或来源填补。

## 6. 后续阶段记录

### 6.1 开发自测

开发自测06：not_started；上述有限检查不替代一期全部验收用例及集成故障验证。

### 6.2 代码审查

代码审查07：not_started；没有独立审查人/代理意见或批准证据，不将编码过程自查记作CR完成。

### 6.3 独立测试与发布

独立测试08与发布09～14：not_started；未发布、未发送真实邮件，无生产样本。当前暂停在05，交付方案与代码供用户审阅。

## 7. SRC-021工程边界整改（当前基线）

前三项约束已同步solo源、三个宿主发行包、portable包与ddd规范1.7/参考快照1.5。Controller上传已移除throws；四层50个实际主入口完整try-catch，无throws声明、无catch重抛。失败Result保留受控错误分类，写事务内部失败触发回滚，提交异常由外层入口捕获。前端已迁至同级hello-travel-app；AI文档/数据库设计材料和知识样本已迁入AI/output，正式迁移/运行源码/脚本位置保留。

| 实际验证 | 结果 | 归档 |
|---|---|---|
| 根Maven install（默认Checkstyle及编译） | BUILD SUCCESS；10条离线测试，失败/错误0；含上传IO异常、非法上传、38个app/domain失败入口 | docs/verification/boundary-backend.log |
| JavaParser主入口审计 | 50/50完整捕获；throws和重抛0 | docs/verification/entry-audit.csv |
| 独立已安装Application消费者 | BUILD SUCCESS，不借用工作区父POM | docs/verification/boundary-consumer.log |
| 同级前端类型/格式/生产构建 | 全部通过；package/lock名称hello-travel-app | docs/verification/app-build.log、app-format.log |
| 禁用后台任务JAR启动/HTTP | UP200；未登录401；非法输入400；缺SMTP503；未发邮件/未调用模型 | docs/verification/boundary-http.json |
| 门禁机制回归 | 7项通过，含三类缺捕获/throws/捕获重抛拒绝案例；流程10、启动14、安装16通过 | ddd的当前边界检查证据及公共工具日志 |

原第4章7条检查为SRC-020历史基线，本轮10条是其上新增3条，不累加宣称17条。SHA与实际迁移读回见boundary-summary.json。06～14仍not_started，Milvus/百炼/高德/SMTP完整联调未执行。

## 8. SRC-023业务方法质量整改（当前版本）

用户指出编号行内注释和面向对象约束未落实，要求检查所有业务类，并要求需求迭代考虑既有完整链路。原FMT-005、DDD-002/003/004已有约定，本次属于修复交付缺口。全量扫描248个Java业务源文件、652个方法/构造器；其中591个实际方法、61个构造器，345个多语句流程块通过编号门禁。基线405个实际方法中240个带控制结构或多语句的方法全部缺少编号阶段，不能把这一事实说成“原代码完全没有任何注释”。

| 规则/问题 | 实际修订 | 验证与限界 |
|---|---|---|
| FMT-005/DOC-007编号与含义 | 公开/私有业务流程、事务及流式回调补充逻辑阶段；构造器的字节复制/预算/查询规则单独说明；纠正邮箱被描述为向量校验等错误注释 | 保存完整method-inventory；编号/规模只证明有限结构，含义另按对象职责复核 |
| DDD-002实体内聚 | 17类Entity持有各自版本/不可迁移归属/删除更新约束，固定初始化与状态复制进入有业务名称的工厂和变更方法 | DomainModelQualityTest对17类实体版本/归属反例，以及账号、标题、消息、任务、会话、事实、分块测试；不覆盖每个生产场景 |
| DDD-003聚合语义 | Aggregate提供创建、更新核验和状态转换；TravelWriteDomainService不再展开entity属性做状态编排 | QUALITY-DOMAIN结构门禁，domain/service调用entity()=0；无仓储/Spring依赖注入聚合 |
| DDD-004应用协调 | 原应用中的19处Entity全字段创建改为领域工厂；应用通过Aggregate发起状态变更，读取快照用于协议/查询仍可保留 | application非assembler显式new Entity=0；Value创建进入实体，有限语法扫描不证明完整类型/职责 |
| 长流程与映射 | MemoryFlow提取拆成调用登记→模型输出记账→事实保存；KnowledgeIndexer按领取、批次索引、删除补偿拆分；文件解析、向量批次和PO映射分离 | 同一JavaParser口径：store55→30、index47→25、execute57→21、extract53→20个非块语句节点；正式JDK门禁最大41，阈值45，两种AST口径不可混用 |
| 迭代清理DEL-007 | 创建与重命名复用实体标题规则，删除新建后再rename的重复更新；原内容/标识/版本在实体变化时明确保持 | 新建与重命名共用不变量、空归属和空邮箱验证证明反例；新增代码服务具体职责，不以增加类/方法数量证明质量 |
| 前端历史对象 | MessageHistory控制全量替换、版本合并、64位稳定序号；修正相同seq比较器不一致，并提供独立快照 | 2项迟到消息/全量替换/防御性复制/大序号测试；React页面继续用函数组合，不为了形式改为类组件 |
| 四层异常与事务既有规范 | 所有主入口继续逐层完整try-catch，无throws/重抛，内部异常仍由所属事务回滚 | 50个入口AST审计0失败、既有提交失败/回滚测试保留 |

新增方法主要用于实体工厂、聚合语义委托、各模型更新约束、持久化映射及流程阶段；它们受八模块和17种持久化模型的真实职责约束。没有引入泛用策略/继承/包装框架。总体代码量会因规则封装和注释增多，不用行数下降作为质量结论。

当前实际验证：后端默认Maven install（Checkstyle+业务扫描+Flatten）16项测试通过；前端2项测试、类型检查、生产构建、格式检查通过。DDD根与独立快照各4项回归通过。solo业务/异常门禁10项、流程10项、启动指导14项、安装16项通过；反例包括漏编号、跳号、非中文描述、过长私有方法、应用创建Entity、领域服务展开entity，以及安装冲突保留。最终发行一致性/独立消费等明细见verification/quality-summary.json。

方法分类及关键链路语义复核说明：[业务方法复核记录](<docs/02 业务方法复核记录.md>)；新默认工具为scripts/JavaBusinessQuality.java（构建运行资产，非AI设计输出）。完整06开发自测与07独立CR保持not_started，05编码终点不变。


当前质量证据是本轮编码整改和针对性开发回归，不是独立CR、完整业务验收或生产认证。真实百炼/嵌入/高德/SMTP、多设备真实同步与整套故障演练未执行。既有迁移不改写，本轮未操作MySQL、Milvus或Redis数据。
