# 本机环境与资料核查

访问时间：2026-09-17T17:29:00+08:00；来源SRC-003；用途为方案可行性及数据设计实验，不是开发自测或独立测试报告。

## 1. 实测环境

- Java17.0.19，Maven3.9.16，Node24.18.0，npm11.16.0；尚无应用工程，不存在构建通过证据。
- MySQL客户端和服务器8.4.11；使用安全解析的zprofile变量在子进程环境中认证，未将密码放命令参数、仓库或输出。
- Redis8.10.1，redis_mode=standalone，6379可连接；未读取/写入现有业务key，不执行FLUSH。
- Docker显示milvus-standalone镜像milvusdb/milvus:v3.0.0，19530可连，9091/healthz返回200/OK；仅查健康，不列/读/删用户已有collections。镜像标签不冒充SDK确认的服务端version。
- zprofile相关变量名已检查：DASHSCOPE_API_KEY、AMAP_MAPS_API_KEY、MYSQL_USERNAME、MYSQL_PASSWORD。只记录名，不记录值，不快照该文件。不假定模型账户/高德/嵌入调用权限已验证。
- 未发现该文件有明示SMTP/MAIL相关变量名；是否存在其他邮件配置未知，未发送真实邮件。

## 2. 用户直接授权的数据库建设

SRC-002要求建设hello-travel数据库，并允许多张表。执行仅创建`hello-travel`专用数据库（utf8mb4/utf8mb4_0900_ai_ci），随后SELECT确认存在、业务表数0。未在其他现有库执行写入。业务建表草案review，正式库当前未建16表。

## 3. 数据设计实验

DDL：`database/design/001_initial_schema.sql`；SHA256：`84c317ab59af47fa066c1f100629f4d1cbd93df094bbe42605efc0a20d8dc558`。

使用本回合新建随机hello-travel-ddl-*临时库测试，测试身份和内容都是example.invalid/设计测试假数据；只DROP该随机临时库并已清理，不DROPhello-travel或其他既有库。

| 实验 | 实际结果 | MySQL预期拒绝码 |
|---|---|---|
| 16表DDL在MySQL8.4实际建表 | PASS | 无 |
| 正常归属数据可写入 | PASS | 无 |
| 同设备第二个ACTIVE登录会话被唯一约束拒绝 | PASS | 1062 |
| 跨用户向他人对话写消息被复合外键拒绝 | PASS | 1452 |
| 同对话重复消息序号被拒绝 | PASS | 1062 |
| 同对话第二个进行中生成任务被拒绝 | PASS | 1062 |
| 非法同步序号被CHECK约束拒绝 | PASS | 3819 |
| 临时库清理 | PASS | 无 |

证明边界：SQL语法、16表可建立和上述约束有效；不证明SELECT鉴权、SSE实时性、token安全、模型/向量/邮箱集成、应用事务或全量恢复功能已实现。

## 4. 依赖元数据核验

直接读取Maven Central公开发布元数据/POM：Boot3.5.16、LangChain4j1.20.0、LangGraph4j core1.8.27、Milvus SDK3.0.x、MyBatis-Plus3.5.17可定位。此为可获得版本，不是完整兼容验证。LangChain4j的Jackson2.22.1及reactive-streaming beta30已记入技术风险。

npm官方registry/latest返回React19.3.0、Ant Design6.6.4、Vite8.3.0、TypeScript7.0.2；Vite Node约束与当前Node匹配。尚无lockfile/tsc/build执行证据。

## 5. 外部来源访问限制

Spring/模型/密码存储/Milvus等官方文档用于草案参数和能力说明，输入清单逐项关联。部分LangGraph/getting-started及高德地理/路线文档链接访问失败，后续实施前须核验实际文档/源码，不能猜API。

景德镇官方预约须知可通过官方检索条目定位；web打开无正文，直接HTTP200仅1334byte且无标题/取消预约正文，属于访问受限，不是已获得正文。没有核验付费门票退改全文，没有导入READY知识数据。

## 编码阶段更新（SRC-020）

前述0.1/0.2实验路径与SHA-256为历史证据。当前唯一DDL已移动到[Flyway V1](../../hello-travel-start/src/main/resources/db/migration/V1__initial_schema.sql)，增加刷新消费账本共17张MySQL业务表，并已迁移专用库；V1执行后冻结。当前迁移/lockfile/JAR校验和及有限构建/启动证据见[verification](../output/docs/verification/README.md)与05。未发送真实邮件或调用收费模型，未初始化Milvus向量集合。
